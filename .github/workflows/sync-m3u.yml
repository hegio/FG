name: Sync Upstream Files

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      # 抑制 Node.js 警告
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Configure Git
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

    - name: Download all upstream files
      run: |
        set -euo pipefail   # ← 新增这一行，提升脚本健壮性
        files=(
          "zilongTV:https://raw.githubusercontent.com/zilong7728/Collect-IPTV/refs/heads/main/best_sorted.m3u8"
          "海豚无18加555:https://raw.githubusercontent.com/FGBLH/GHK/refs/heads/main/%E6%B5%B7%E8%B1%9A%E6%97%A018%E5%8A%A0555.json"
          "海豚666:https://raw.githubusercontent.com/FGBLH/GHK/refs/heads/main/%E6%B5%B7%E8%B1%9A666.json"
          "鱼壳海豚:https://raw.githubusercontent.com/FGBLH/GHK/refs/heads/main/%E9%B1%BC%E5%A3%B3%E6%B5%B7%E8%B1%9A.json"
        )
        
        for item in "${files[@]}"; do
          name="${item%%:*}"
          url="${item##*:}"
          
          echo "👉 Downloading $name ..."
          
          if curl -L --fail --max-time 30 -o "$name.tmp" "https://ghfast.top/$url"; then
            echo "✅ $name 下载成功（ghfast）"
          elif curl -L --fail --max-time 30 -o "$name.tmp" "$url"; then
            echo "✅ $name 下载成功（direct）"
          else
            echo "::warning::Failed to download $name – skipping"
            continue
          fi
          
          if [ -s "$name.tmp" ]; then
            mv "$name.tmp" "$name"
            echo "📦 $name 已保存 ($(wc -l < $name 2>/dev/null || echo '?') 行) "
          else
            echo "::warning::$name 内容为空，已删除"
            rm -f "$name.tmp"
          fi
        done

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Add external live sources to 海豚影视
      run: |
        cat > add_sources.py << 'EOF'
        import json, sys, os

        def add_live_sources():
            src_file = '海豚影视'
            if not os.path.isfile(src_file):
                print(f"[WARN] {src_file} 不存在，跳过添加直播源")
                return False

            try:
                with open(src_file, 'r', encoding='utf-8') as f:
                    data = json.load(f)
            except Exception as e:
                print(f"[ERROR] 读取 {src_file} 失败: {e}")
                return False

            new_sources = [
                {
                    "name": "🐬海角黄色",
                    "type": 0,
                    "ua": "okhttp",
                    "url": "https://ghfast.top/https://raw.githubusercontent.com/FGBLH/fgrjk2/main/haijiao669"
                },
                {
                    "name": "🐬zilong大陆TV",
                    "type": 0,
                    "ua": "okhttp",
                    "url": "https://ghfast.top/https://raw.githubusercontent.com/zilong7728/Collect-IPTV/main/best_sorted.m3u8"
                }
            ]
    
            existed = {i.get('name', '') for i in data.get('lives', [])}
            added = 0
            for src in new_sources:
                if src['name'] not in existed:
                    data.setdefault('lives', []).append(src)
                    print(f"[INFO] Added {src['name']}")
                    added += 1
                else:
                    print(f"[WARN] 已存在 {src['name']}")

            if added:
                with open(src_file, 'w', encoding='utf-8') as f:
                    json.dump(data, f, ensure_ascii=False, indent=4)
                print(f"[INFO] 共新增 {added} 条直播源")
                return True
            return False

        if __name__ == '__main__':
            sys.exit(0 if add_live_sources() else 0)
        EOF
        python3 add_sources.py

    # ── 6️⃣ 生成鱼壳海豚_local.json（本地 spider.jar 挂载） ──
    - name: Generate 鱼壳海豚_local.json
      run: |
        cat > generate_local.py << 'EOF'
        import json
        import os

        src = "鱼壳海豚"
        dst = "鱼壳海豚_local.json"

        if not os.path.isfile(src):
            print(f"[WARN] {src} 不存在，跳过生成 local 版本")
            exit(0)

        with open(src, 'r', encoding='utf-8') as f:
            data = json.load(f)

        # 设置本地 spider.jar 路径（Termux 实际位置）
        data["spider"] = "/data/data/com.termux/files/home/spider.jar"
        data["wallpaper"] = "http://tool.teyonds.com/api"
        data["logo"] = "https://img.freepik.com/free-vector/cute-dolphin-swimming-cartoon-vector-icon-illustration-animal-nature-icon-isolated-flat-vector_138676-12582.jpg?semt=ais_hybrid&w=740&q=80"

        with open(dst, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=4)

        print(f"[DONE] 已生成 {dst}（本地挂载）")
        print(f"[INFO] spider 路径: /data/data/com.termux/files/home/spider.jar")
        EOF
        python3 generate_local.py

    # ── 7️⃣ 生成鱼壳海豚_gb.json（GitHub spider.jar 挂载） ──
    - name: Generate 鱼壳海豚_gb.json
      run: |
        cat > generate_gb.py << 'EOF'
        import json
        import os   # ← 新增这一行

        src = "鱼壳海豚"
        dst = "鱼壳海豚_gb.json"

        if not os.path.isfile(src):
            print(f"[WARN] {src} 不存在，跳过生成 GB 版本")
            exit(0)

        with open(src, 'r', encoding='utf-8') as f:
            data = json.load(f)

        # 设置 GitHub 托管的 spider.jar 路径
        # 使用 ghfast.top 加速访问
        data["spider"] = "https://ghfast.top/https://raw.githubusercontent.com/hegio/FG/main/spider.jar"
        data["wallpaper"] = "http://tool.teyonds.com/api"
        data["logo"] = "https://img.freepik.com/free-vector/cute-dolphin-swimming-cartoon-vector-icon-illustration-animal-nature-icon-isolated-flat-vector_138676-12582.jpg?semt=ais_hybrid&w=740&q=80"

        with open(dst, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=4)

        print(f"[DONE] 已生成 {dst}（GitHub 挂载）")
        print(f"[INFO] spider URL: https://ghfast.top/https://raw.githubusercontent.com/hegio/FG/main/spider.jar")
        EOF
        python3 generate_gb.py

    # ── 8️⃣ 生成综合 FgPlayList.m3u ──
    - name: Generate comprehensive FgPlayList.m3u
      run: |
        cat > generate_FgPlayList.py << 'EOF'
        #!/usr/bin/env python3
        import os, json, base64, re, urllib.parse

        def parse_m3u(file_path, prefix=""):
            if not os.path.isfile(file_path):
                return []
            channels = []
            with open(file_path, 'r', encoding='utf-8') as f:
                lines = f.readlines()
            i = 0
            while i < len(lines):
                line = lines[i].strip()
                if line.startswith('#EXTINF:-1'):
                    name_match = re.search(r',(.*)$', line)
                    name = name_match.group(1).strip() if name_match else "未知"
                    j = i + 1
                    while j < len(lines):
                        nxt = lines[j].strip()
                        if nxt and not nxt.startswith('#'):
                            if nxt.startswith(('http','rtmp','rtsp','p2p')):
                                full_name = f"{prefix} {name}" if prefix else name
                                channels.append((full_name, nxt))
                            break
                        j += 1
                i += 1
            return channels

        def extract_from_json(file_path):
            if not os.path.isfile(file_path):
                return []
            with open(file_path, 'r', encoding='utf-8') as f:
                data = json.load(f)

            result = []
            for item in data.get('lives', []):
                url = item.get('url') or item.get('link') or item.get('stream')
                name = item.get('name') or item.get('title') or '未知'
                if url:
                    result.append((name, url))
            return result

        all_channels = []

        # M3U 文件
        m3u_sources = [
            ("zilongTV", "🐬"),
            ("海豚无18加555", "🐬"),
        ]
        for fname, prefix in m3u_sources:
            if os.path.isfile(fname):
                ch = parse_m3u(fname, prefix)
                all_channels.extend(ch)
                print(f"[INFO] {fname}: 解析到 {len(ch)} 条频道")

        # JSON（所有三个版本）
        json_sources = ["海豚666", "鱼壳海豚", "鱼壳海豚_local", "鱼壳海豚_gb"]
        for fname in json_sources:
            if os.path.isfile(fname):
                ch = extract_from_json(fname)
                all_channels.extend(ch)
                print(f"[INFO] {fname}: 解析到 {len(ch)} 条直播源")

        # 生成 FgPlayList.m3u
        out_path = "FgPlayList.m3u"
        with open(out_path, "w", encoding="utf-8") as out:
            out.write("#EXTM3U\n")
            out.write('#EXTINF:-1 tvg-name="🐬海豚影视综合",🐬海豚影视综合\n')
            out.write("http://127.0.0.1/placeholder\n")
            
            for name, url in all_channels:
                clean_name = re.sub(r'[|\\/*?:<>"]', '', name)
                out.write(f'#EXTINF:-1 tvg-name="{clean_name}",{clean_name}\n')
                out.write(f'{url}\n')

        print(f"[DONE] 生成 {out_path}，共计 {len(all_channels)} 条频道")
        EOF
        python3 generate_FgPlayList.py

    - name: Convert specific M3U files (keep YouTube links)
      run: |
        cat > convert.py << 'EOF'
        import os, re
        def convert_to_m3u(src, dst):
            if not os.path.isfile(src):
                print(f"[WARN] {src} 不存在，跳过")
                return
            with open(src, 'r', encoding='utf-8') as f:
                lines = f.readlines()
            out = ['#EXTM3U x-tvg-url=""']
            group = '未分类'
            cnt = 0
            for line in lines:
                line = line.strip()
                if not line:
                    continue
                if ',#genre#' in line:
                    group = line.replace(',#genre#', '').strip()
                    continue
                for proto in [',http://', ',https://', ',p2p://', ',rtmp://', ',rtsp://']:
                    if proto in line:
                        idx = line.find(proto)
                        name = line[:idx].strip()
                        url  = line[idx+1:].strip()
                        name = re.sub(r'\s+', ' ', name)
                        out.append(f'#EXTINF:-1 group-title="{group}",{name}')
                        out.append(url)
                        cnt += 1
                        break
            with open(dst, 'w', encoding='utf-8') as f:
                f.write('\n'.join(out))
            print(f"[DONE] {src} → {dst} ({cnt} 条）")
        
        convert_to_m3u("zilongTV", "zilongTV.m3u")
        convert_to_m3u("海豚无18加555", "haitun.m3u")
        EOF
        python3 convert.py

    - name: Commit and push changes
      run: |
        git add -A
        if git diff --staged --quiet; then
          echo "✅ 没有需要提交的改动"
        else
          git commit -m "🔄 Sync: Generate local and GB versions [$(date '+%Y-%m-%d %H:%M')]
          - 同步所有上游文件
          - 生成鱼壳海豚_local.json（本地 spider.jar）
          - 生成鱼壳海豚_gb.json（GitHub spider.jar）
          - 生成综合 FgPlayList.m3u"
          git push
          echo "🚀 提交已完成"
        fi
